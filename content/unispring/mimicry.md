---
title: Mimicry (renamed)
nav_exclude: true
sitemap: false
search_exclude: true
---

# This page has moved

The Mimicry factory has been renamed to **[Notable](/notable/)**, and Notable now lives in its own top-level section.

- New page: [Notable](/notable/)
- New mechanics page: [Peg mechanics](/notable/mechanics/)

The `mimic(name)` write function is now `issue(name)`, and the `mimicked(...)` read functions are now `issued(...)`. The `make(original, symbol)` signature is unchanged.
